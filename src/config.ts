export const CONFIG = {
    PACKET_SPEED: 8, // Pixels per frame
    START_DELAY: 60, // 2 seconds delay at 30fps
    FRAMES_PER_LOG: 120, // Duration each log is "active" logic-wise
    LAYOUT: {
        RADIUS: 350,
        USER_NODE_X: 300,
        USER_NODE_Y: 250,
        BRAIN_RADIUS: 75,
        SATELLITE_RADIUS: 30,
        USER_RADIUS: 40,
    }
};
